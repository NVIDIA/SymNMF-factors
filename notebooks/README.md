# Notebooks

Run notebooks from the repository root after installing `.[notebook]`:

```bash
jupyter lab notebooks/
```

`clustering_through_time.ipynb` uses relative `data/` paths and the installed
`adaptgrow` package. Clear execution outputs before committing notebook changes.
